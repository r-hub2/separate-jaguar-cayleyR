% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_processing.R
\name{save_bridge_states}
\alias{save_bridge_states}
\title{Save Bridge States to CSV}
\usage{
save_bridge_states(bridge_states, filename)
}
\arguments{
\item{bridge_states}{List of lists, each containing \code{state} (integer vector)
and \code{cycle} (integer)}

\item{filename}{Character, output CSV file path}
}
\value{
Invisible NULL. Side effect: writes a CSV file.
}
\description{
Writes a list of bridge states (each with \code{state} and \code{cycle} fields)
to a CSV file.
}
\examples{
bs <- list(
  list(state = 1:5, cycle = 0),
  list(state = c(2, 1, 3, 4, 5), cycle = 1)
)
# save_bridge_states(bs, tempfile(fileext = ".csv"))
}
