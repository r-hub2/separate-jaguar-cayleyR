% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_metrics.R
\name{cayley_graph_diameter}
\alias{cayley_graph_diameter}
\title{Cayley Graph Diameter and Maximally Distant State Pairs}
\usage{
cayley_graph_diameter(
  start_state,
  k,
  moves = c("L", "R", "X"),
  method = c("all_pairs", "from_start"),
  max_pairs = Inf,
  verbose = FALSE
)
}
\arguments{
\item{start_state}{Integer vector, the state to explore from}

\item{k}{Integer, parameter for the reverse-prefix operation}

\item{moves}{Character vector of allowed operations (default: c("L","R","X"))}

\item{method}{Either "all_pairs" (default, exact) or "from_start" (single
BFS, exact only for vertex-transitive graphs)}

\item{max_pairs}{Numeric, maximum number of pairs to materialise in
\code{pairs_df} (default \code{Inf}). \code{n_pairs} always reports the honest total.}

\item{verbose}{Logical; if TRUE, prints progress during the sweep}
}
\value{
List containing:
\item{diameter}{Integer, the graph diameter (or start eccentricity)}
\item{n_vertices}{Number of reachable states}
\item{n_pairs}{Total number of maximally distant pairs found}
\item{truncated}{Logical, TRUE if \code{pairs_df} was capped by \code{max_pairs}}
\item{pairs_df}{Data frame of maximally distant pairs, with the celestial
coordinates of both endpoints (\verb{from_*} and \verb{to_*} columns)}
\item{ecc}{Data frame of per-vertex eccentricities (all \code{NA} except the
start vertex when \code{method = "from_start"})}
\item{bfs}{The full BFS data frame from \code{start_state}, as returned by
\code{\link{cayley_bfs_full}}}
\item{dist_hist}{Data frame of distance-from-start counts}
\item{method}{The method actually used}
}
\description{
Computes the diameter of the Cayley graph component reachable from
\code{start_state}, together with the pairs of states realising it and the
eccentricity of each vertex.
}
\details{
Two methods are available. \code{"all_pairs"} runs a BFS from every vertex and
yields the true diameter and every diametral pair; its cost grows as the
number of vertices times the cost of one BFS, which in practice limits it to
permutations of roughly size 8 or below. \code{"from_start"} runs a single BFS and
reports the eccentricity of \code{start_state} and the pairs \verb{(start_state, v)}
realising it; this equals the diameter only when the graph is
vertex-transitive, but it scales to much larger graphs.
}
\examples{
res <- cayley_graph_diameter(1:5, k = 3)
res$diameter
head(res$pairs_df)
}
\seealso{
\code{\link{cayley_bfs_full}}
}
